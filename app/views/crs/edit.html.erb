<% provide(:title,'Ctrl_edit') %>
<%= form_for(@cr) do |f|%>
  <div class="col-md-12">
  <%= f.hidden_field :id%>
  <%=f.label :officer, 'Officer in charge'%>
  <%=f.label :officer, @current_user.Name %>
  <div class="actions">
  <%= f.submit %>
  </div>
  </div>
<% end %>  
  <div class="col-md-12">
    <div class="row">
        <%= @cr.division %>
        <%= @cr.Name %>
        <%= @cr.ext %>
        <%= @cr.cr_type %>
        <%= @cr.Accused %>
    </div>
  </div>

